# Test 5 — question 6



> Source : [document original](../../sources/AZ-104-Practice-Test-5.docx). Les réponses et explications ci-dessous proviennent du document fourni et restent à vérifier. Les éventuelles figures sont dans le DOCX original.



## Énoncé et corrigé fourni


You have two Azure virtual machines as shown in the following table. Each virtual machine runs a different operating system, has both private and public IP addresses assigned, and is connected to the same virtual network. One virtual machine has a DNS suffix configured in the operating system, while the other does not.
You then create the following Azure DNS resources:
- A public DNS zone named contoso.com
- A private DNS zone named fabrikam.com
You perform the following actions:
- For fabrikam.com, you create a virtual network link to vnet1 and enable automatic registration.
- For contoso.com, you assign the Owner role to both vm1 and vm2.
True of False:
“The DNS A record for vm1 is created in fabrikam.com and resolves to the IP address 10.0.1.4.”
Réponses:
**☑** True
☐ False
Explication
`True` is correct because fabrikam.com is a private DNS zone, and since auto-registration is enabled for vnet1, the private IP address (10.0.1.4) of vm1 will be automatically added as an A record to the fabrikam.com private DNS zone. Auto-registration is supported for private DNS zones, and linking the virtual network ensures that virtual machines connected to the network can have their private IP addresses automatically registered in the private DNS zone.
Reference(s):
- https://learn.microsoft.com/en-us/azure/dns/private-dns-autoregistration

## Analyse vérifiée

- **Statut :** à vérifier avec Microsoft Learn.
- **Fait décisif :**
- **Pourquoi la réponse est correcte :**
- **Pourquoi les autres choix sont incorrects ici :**
- **Quand chaque autre choix serait correct :**
- **Schéma ou tableau :**
- **Règle à retenir :**
- **Sources Microsoft Learn :**
