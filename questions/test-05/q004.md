# Test 5 — question 4



> Source : [document original](../../sources/AZ-104-Practice-Test-5.docx). Les réponses et explications ci-dessous proviennent du document fourni et restent à vérifier. Les éventuelles figures sont dans le DOCX original.



## Énoncé et corrigé fourni


You have an Azure subscription named Sub1 that includes several blob containers, as shown in the following table.
Name           In storage account         Contains blob**
-------         -------------------         ---------------
cont1            storage1                            blob1
cont2           storage1                            blob2
cont1            storage2                           blob3
Each container resides in a specific storage account and contains one blob.
The subscription includes two users named User1 and User2. Both users are assigned the Reader role at the Sub1 subscription scope.
You have defined a condition named `Condition1` with the following logic:
- The condition evaluates to true when the requested action is not a blob read operation
OR
- The container name equals cont1
You have also defined a second condition named `Condition2` with the following logic:
- The condition evaluates to true when the requested action is not a blob write operation
OR
- The blob path matches a pattern that contains the value 2
You then assign roles to the users:
- User1 is assigned the Storage Blob Data Reader role at the Sub1 scope with Condition1 applied.
- User2 is assigned the Storage Blob Data Owner role at the storage1 account scope with Condition2 applied.
True or False:
“User2 can read blob1.”
Réponses:
**☑** True
☐ False
Explication
`True` is correct. User2 is assigned the Storage Blob Data Owner role at the storage1 scope, which includes blob1 and grants permissions to read and write blob data, subject to Condition2.
Condition2 evaluates to true when either the requested action is not a blob write operation or the blob path contains the value `2`. Because reading blob1 is not a write operation, the first clause evaluates to true. Although blob1’s path does not contain `2`, the complete expression evaluates as `true OR false`, which is true.
Therefore, Condition2 permits the read operation, and User2 can read blob1. The blob-path requirement would only restrict write operations, because for a write operation the first clause would evaluate to false.
Reference(s):
- https://learn.microsoft.com/en-us/azure/role-based-access-control/conditions-format

## Analyse vérifiée

- **Statut :** à vérifier avec Microsoft Learn.
- **Fait décisif :**
- **Pourquoi la réponse est correcte :**
- **Pourquoi les autres choix sont incorrects ici :**
- **Quand chaque autre choix serait correct :**
- **Schéma ou tableau :**
- **Règle à retenir :**
- **Sources Microsoft Learn :**
