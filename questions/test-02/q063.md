# Test 2 — question 63



> Source : [document original](../../sources/AZ-104-Practice-Test-2.docx). Les réponses et explications ci-dessous proviennent du document fourni et restent à vérifier. Les éventuelles figures sont dans le DOCX original.



## Énoncé et corrigé fourni


You have an Azure subscription that includes a resource group named RG1.
You intend to deploy resources to RG1 by using an Azure Resource Manager (ARM) template named template1. The deployment must meet the following conditions:
- Deploy new resources to RG1.
- All existing resources in RG1 must be deleted before deployment.
You are using the following Azure PowerShell command, which is currently incomplete:
Which value should you choose for `Code Segment A`
Réponses:
☐ -Name
☐ -QueryString
**☑** -ResourceGroupName
☐ -Tag
Explication
`-ResourceGroupName` is the correct option because when deploying an Azure Resource Manager (ARM) template to a specific resource group using the New-AzResourceGroupDeployment command, the -ResourceGroupName parameter is used to specify the target resource group where resources will be deployed.
`-Name` is incorrect because this parameter is used to provide a deployment name, not a resource group name.
`-QueryString` is incorrect because it is not a valid parameter for specifying the target resource group in the context of New-AzResourceGroupDeployment.
`-Tag` is incorrect because it is used to assign metadata tags to resources, not to specify the resource group.
Reference(s):
- https://learn.microsoft.com/en-us/powershell/module/az.resources/new-azresourcegroupdeployment
- https://learn.microsoft.com/en-us/azure/azure-resource-manager/templates/deployment-manager-overview

## Analyse vérifiée

- **Statut :** à vérifier avec Microsoft Learn.
- **Fait décisif :**
- **Pourquoi la réponse est correcte :**
- **Pourquoi les autres choix sont incorrects ici :**
- **Quand chaque autre choix serait correct :**
- **Schéma ou tableau :**
- **Règle à retenir :**
- **Sources Microsoft Learn :**
