# Test 2 — question 34



> Source : [document original](../../sources/AZ-104-Practice-Test-2.docx). Les réponses et explications ci-dessous proviennent du document fourni et restent à vérifier. Les éventuelles figures sont dans le DOCX original.



## Énoncé et corrigé fourni


You are designing an Azure deployment that uses Azure Resource Manager (ARM) templates to provision 50 virtual machines. All virtual machines will be placed in the same availability set.
The goal of the design is to maximize the number of virtual machines that remain online during either Azure platform failures or planned maintenance events.
To achieve this, you are configuring an ARM template that defines an availability set, as shown in the exhibit. The template includes settings for fault domains and update domains.
Which value should you specify for the `platformUpdateDomainCount` property in the availability set configuration?
Réponses:
☐ 0
☐ 1
☐ 6
☐ 12
☐ 17
**☑** 20
Explication
`20` is correct because Azure Availability Sets support up to 20 update domains by default. Update domains are used to ensure that virtual machines in the same availability set do not go down for maintenance at the same time. By setting the platformUpdateDomainCount to 20, you maximize the distribution of virtual machines across update domains, which helps maintain availability during planned maintenance events.
`0`, `1`, `6`, `12`, and `17` are incorrect because:
- 0 would mean no update domain distribution, resulting in all VMs potentially being impacted by maintenance at the same time.
- 1, 6, 12, and 17 would offer fewer update domains, which would not maximize availability during updates as effectively as using 20.
Reference(s):
- https://learn.microsoft.com/en-us/azure/virtual-machines/maintenance-and-updates
- https://learn.microsoft.com/en-us/azure/virtual-machines/availability-set-overview

## Analyse vérifiée

- **Statut :** à vérifier avec Microsoft Learn.
- **Fait décisif :**
- **Pourquoi la réponse est correcte :**
- **Pourquoi les autres choix sont incorrects ici :**
- **Quand chaque autre choix serait correct :**
- **Schéma ou tableau :**
- **Règle à retenir :**
- **Sources Microsoft Learn :**
