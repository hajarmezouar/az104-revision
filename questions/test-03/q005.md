# Test 3 — question 5



> Source : [document original](../../sources/AZ-104-Practice-Test-3.docx). Les réponses et explications ci-dessous proviennent du document fourni et restent à vérifier. Les éventuelles figures sont dans le DOCX original.



## Énoncé et corrigé fourni


You have an Azure subscription that includes two storage accounts named contoso101 and contoso102.
The subscription also contains two virtual machines with the following configurations:
- VM1 is connected to VNet1/Subnet1 and uses a Basic public IP address SKU.
- VM2 is connected to VNet1/Subnet2 and uses a Standard public IP address SKU.
Within VNet1, service endpoints are configured as follows:
- Microsoft.AzureActiveDirectory is enabled on Subnet2.
- Microsoft.Storage is enabled on Subnet1.
A service endpoint policy named Policy1 is created for the Microsoft.Storage service. The policy allows access only to the storage account contoso101. The policy is associated with subnets that have Microsoft.Storage service endpoints enabled.
True or False:
`“VM1 can access contoso102.”`
Réponses:
☐ True
**☑** False
Explication
`False` is the correct answer here. According to the information provided, the service endpoint policy is configured to allow access only to the contoso101 storage account through the Microsoft.Storage service endpoint associated with Subnet1 in VNet1. Since VM1 is connected to Subnet1 and the policy only permits access to contoso101, VM1 cannot access contoso102 because it is not included in the endpoint policy.
Reference(s):
- https://learn.microsoft.com/en-us/azure/virtual-network/virtual-network-service-endpoints-overview
- https://learn.microsoft.com/en-us/azure/virtual-network/virtual-network-service-endpoint-policies?tabs=portal

## Analyse vérifiée

- **Statut :** à vérifier avec Microsoft Learn.
- **Fait décisif :**
- **Pourquoi la réponse est correcte :**
- **Pourquoi les autres choix sont incorrects ici :**
- **Quand chaque autre choix serait correct :**
- **Schéma ou tableau :**
- **Règle à retenir :**
- **Sources Microsoft Learn :**
