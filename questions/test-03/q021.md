# Test 3 — question 21



> Source : [document original](../../sources/AZ-104-Practice-Test-3.docx). Les réponses et explications ci-dessous proviennent du document fourni et restent à vérifier. Les éventuelles figures sont dans le DOCX original.



## Énoncé et corrigé fourni


You have a public load balancer that distributes traffic on ports 80 and 443 across three virtual machines named VM1, VM2, and VM3.
You want all Remote Desktop Protocol (RDP) connections to be routed exclusively to VM3.
What configuration should you apply?
Réponses:
☐ Create a separate public load balancer dedicated to VM3
☐ Configure an additional frontend IP configuration
**☑** Configure an inbound NAT rule
☐ Configure a load balancing rule
Explication
`Configure an inbound NAT rule` is correct because an inbound NAT rule allows you to direct specific traffic, such as RDP, to a particular virtual machine (VM3 in this case) behind the load balancer. By setting up an inbound NAT rule for port 3389 (RDP), you ensure that all RDP connections go directly to VM3 instead of balancing across all VMs.
`Create a separate public load balancer dedicated to VM3` is incorrect because creating a separate load balancer is unnecessary for directing RDP connections to VM3. An inbound NAT rule is sufficient to achieve this without additional load balancers.
`Configure an additional frontend IP configuration` is incorrect as a frontend IP configuration is used to define the IP address on which the load balancer is accessible. It does not control how RDP traffic is directed to specific VMs.
`Configure a load balancing rule` is incorrect because load balancing rules are intended for distributing traffic across multiple VMs based on specific ports, which is not the goal here. The objective is to direct all RDP traffic to VM3 specifically, not balance it across VMs.
Reference(s):
- https://learn.microsoft.com/en-us/azure/load-balancer/tutorial-load-balancer-port-forwarding-portal

## Analyse vérifiée

- **Statut :** à vérifier avec Microsoft Learn.
- **Fait décisif :**
- **Pourquoi la réponse est correcte :**
- **Pourquoi les autres choix sont incorrects ici :**
- **Quand chaque autre choix serait correct :**
- **Schéma ou tableau :**
- **Règle à retenir :**
- **Sources Microsoft Learn :**
