# Test 3 — question 9



> Source : [document original](../../sources/AZ-104-Practice-Test-3.docx). Les réponses et explications ci-dessous proviennent du document fourni et restent à vérifier. Les éventuelles figures sont dans le DOCX original.



## Énoncé et corrigé fourni


You have an Azure subscription that contains the following resources:
- A virtual network named VNET1, which includes two subnets.
- subnet1 uses the IP address range 10.3.0.0/24.
- subnet2 uses the IP address range 10.4.0.0/24.
- A network security group named NSG1.
- A virtual machine named vm1 that has the private IP address 10.3.0.15 and is deployed in subnet1.
- A virtual machine named vm2 that has the private IP address 10.4.0.16 and is deployed in subnet2.
- A storage account named storage1.
The network security group NSG1 is configured with custom security rules as follows:
True or False:
`“VM2 can access VM1 by using the HTTPS protocol.”`
Réponses:
**☑** True
☐ False
Explication
`True` is the correct answer here. The HTTPS_VM1_Deny rule with priority 110 specifically blocks HTTPS (TCP port 443) traffic only from the Internet to VM1. Since VM2 is within the same virtual network (VNet1), the default VNet-to-VNet communication rule will allow HTTPS traffic between VM2 and VM1. Therefore, VM2 can access VM1 using the HTTPS protocol.
Reference(s):
- https://docs.microsoft.com/en-us/azure/virtual-network/network-security-groups-overview
- https://docs.microsoft.com/en-us/azure/virtual-network/service-tags-overview

## Analyse vérifiée

- **Statut :** à vérifier avec Microsoft Learn.
- **Fait décisif :**
- **Pourquoi la réponse est correcte :**
- **Pourquoi les autres choix sont incorrects ici :**
- **Quand chaque autre choix serait correct :**
- **Schéma ou tableau :**
- **Règle à retenir :**
- **Sources Microsoft Learn :**
