# Test 6 — question 8



> Source : [document original](../../sources/AZ-104-Practice-Test-6.docx). Les réponses et explications ci-dessous proviennent du document fourni et restent à vérifier. Les éventuelles figures sont dans le DOCX original.



## Énoncé et corrigé fourni


Your environment includes an on-premises Active Directory Domain Services (AD DS) domain with the identities listed below:
- User1 is a user account located in OU2.
- User2 is a user account located in OU1.
- Group1 is a global security group that includes User1 and is located in OU1.
You have an Azure subscription that contains a storage account named storage1.
The Azure Files shares in storage1 use AD DS as the identity source, and the default share-level permissions are configured to allow access for all authenticated users and groups.
You create an Azure Files share named share1 and assign the following roles:
- User2 is assigned the Storage File Data SMB Share Reader role.
- Group1 is assigned the Storage File Data SMB Share Contributor role.
You also have a Microsoft Entra tenant that includes a cloud-only user named User3.
Microsoft Entra Connect is configured to synchronize OU1 from the on-premises AD DS domain to the Microsoft Entra tenant.
True or False:
`"User3 can access content in share1."`
Réponses:
☐ True
**☑** False
Explication
`False` is correct. For AD DS-based Azure Files authentication, Azure Files uses Kerberos through the configured identity source, and Microsoft states that only hybrid identities that exist in both on-premises AD DS and Microsoft Entra ID can be authenticated and authorized for Azure file share access. Also, a storage account can use only one identity source for SMB identity-based authentication, so cloud-only access would require a different identity source such as Microsoft Entra Kerberos, not AD DS.
Reference(s):
- https://learn.microsoft.com/en-us/azure/storage/files/storage-files-identity-ad-ds-assign-permissions?tabs=azure-portal

## Analyse vérifiée

- **Statut :** à vérifier avec Microsoft Learn.
- **Fait décisif :**
- **Pourquoi la réponse est correcte :**
- **Pourquoi les autres choix sont incorrects ici :**
- **Quand chaque autre choix serait correct :**
- **Schéma ou tableau :**
- **Règle à retenir :**
- **Sources Microsoft Learn :**
