# Test 6 — question 34



> Source : [document original](../../sources/AZ-104-Practice-Test-6.docx). Les réponses et explications ci-dessous proviennent du document fourni et restent à vérifier. Les éventuelles figures sont dans le DOCX original.



## Énoncé et corrigé fourni


You have an Azure virtual network named VNet1, as shown in the exhibit.
VNet1 has an address space of 10.2.0.0/16, and currently no devices are connected to it.
You plan to create a virtual network peering between VNet1 and another virtual network named VNet2. VNet2 also uses the address space 10.2.0.0/16.
You need to establish the peering between the two virtual networks.
What action should you perform first?
Réponses:
☐ Add a gateway subnet to VNet1.
☐ Create a subnet on VNet1 and VNet2.
**☑** Modify the address space of VNet1.
☐ Configure a service endpoint on VNet2.
Explication
`Modify the address space of VNet1` is correct because the address spaces of two virtual networks being peered must not overlap. In this case, both VNet1 and VNet2 have the address space 10.2.0.0/16, which causes an overlap and prevents peering. To enable peering, you would first need to modify the address space of VNet1 to a non-overlapping range.
`Add a gateway subnet to VNet1` is incorrect because a gateway subnet is only required for VPN Gateway connections, not for simple VNet peering.
`Create a subnet on VNet1 and VNet2` is incorrect because the presence of subnets is not a requirement for VNet peering. The issue here is the overlapping address space, which must be resolved first.
`Configure a service endpoint on VNet2` is incorrect because service endpoints are used to secure access to Azure services and are not related to VNet peering configuration.
Reference(s):
- https://learn.microsoft.com/en-us/azure/virtual-network/virtual-network-manage-peering

## Analyse vérifiée

- **Statut :** à vérifier avec Microsoft Learn.
- **Fait décisif :**
- **Pourquoi la réponse est correcte :**
- **Pourquoi les autres choix sont incorrects ici :**
- **Quand chaque autre choix serait correct :**
- **Schéma ou tableau :**
- **Règle à retenir :**
- **Sources Microsoft Learn :**
