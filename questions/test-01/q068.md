# Test 1 — question 68



> Source : [document original](../../sources/AZ-104-Practice-Test-1.docx). Les réponses et explications ci-dessous proviennent du document fourni et restent à vérifier. Les éventuelles figures sont dans le DOCX original.



## Énoncé et corrigé fourni


Note: This question is part of a series that uses the same overall scenario. Each question in the series proposes a different solution that may or may not satisfy the stated requirement.
You have an application named App1 that runs on two Azure virtual machines, VM1 and VM2. Incoming traffic to App1 is handled through an Azure Load Balancer. The effective network security rules applied to VM2 are shown in the exhibit.
You observe that connection attempts from the IP address 131.107.100.50 to App1 over TCP port 443 are unsuccessful, even though the Load Balancer rules have been confirmed to be correctly configured.
You want to ensure that traffic from 131.107.100.50 can successfully reach App1 on port 443.
Proposed Solution: You create an inbound security rule that allows any traffic from the AzureLoadBalancer source and has a priority of 150.
Does this solution achieve the stated objective?
Réponses:
**☑** Yes
☐ No
Explication
`Yes` is correct because adding a new inbound rule at priority 150 that explicitly allows traffic from the AzureLoadBalancer service tag ensures that health probe traffic is not blocked by the existing deny rule at priority 200. With this higher-priority rule in place, the health probe can once again mark VM2 as healthy, enabling the load balancer to forward traffic to it. As in Q1, once the probe succeeds, the existing allow rule at priority 100 for 131.107.100.50 ensures the client connection over TCP 443 is successful.
Reference(s):
- https://learn.microsoft.com/en-us/azure/load-balancer/manage-rules-how-to

## Analyse vérifiée

- **Statut :** à vérifier avec Microsoft Learn.
- **Fait décisif :**
- **Pourquoi la réponse est correcte :**
- **Pourquoi les autres choix sont incorrects ici :**
- **Quand chaque autre choix serait correct :**
- **Schéma ou tableau :**
- **Règle à retenir :**
- **Sources Microsoft Learn :**
