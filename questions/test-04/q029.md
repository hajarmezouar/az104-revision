# Test 4 — question 29



> Source : [document original](../../sources/AZ-104-Practice-Test-4.docx). Les réponses et explications ci-dessous proviennent du document fourni et restent à vérifier. Les éventuelles figures sont dans le DOCX original.



## Énoncé et corrigé fourni


You have an Azure App Service named AppService01 that includes two folders: LogsFolder and TempFolder.
You must configure a daily backup of AppService01, but ensure that TempFolder is excluded from the backup process.
What should you create first to meet this requirement?
Réponses:
**☑** An Azure Storage account
☐ A Backup vault
☐ A Recovery Services vault
☐ A resource group
Explication
`An Azure Storage account` is the correct answer because Azure App Service backups require an Azure Storage account to store the backup data. When configuring the backup, you can specify which files and folders to include or exclude, allowing you to exclude TempFolder from the backup of WebApp1.
`A Backup vault` is incorrect because App Service backups do not use Backup vaults. They rely on Azure Storage accounts.
`A Recovery Services vault` is incorrect because Recovery Services vaults are used for Azure Backup and Azure Site Recovery, not for App Service backups.
`A resource group` is incorrect because, while required for organizing resources, it is not specifically needed to configure backups for App Service.
Reference(s):
- https://learn.microsoft.com/en-us/azure/app-service/manage-backup

## Analyse vérifiée

- **Statut :** à vérifier avec Microsoft Learn.
- **Fait décisif :**
- **Pourquoi la réponse est correcte :**
- **Pourquoi les autres choix sont incorrects ici :**
- **Quand chaque autre choix serait correct :**
- **Schéma ou tableau :**
- **Règle à retenir :**
- **Sources Microsoft Learn :**
